#include "./Lexer.hpp"

namespace Klang {
    std::vector<Token> tokenize(std::string source) {
        std::vector<Token> tokens;

        // TODO: Process each character in source string to generate tokens

        return tokens;
    }
} // namespace Klang